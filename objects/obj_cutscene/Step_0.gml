camera_set_view_pos(view_camera[0],xpos,180);
//what this means is that xpos will subtract 0.3 each step until it would be less then 0 in which case it would set to 0
xpos = max(xpos - 0.3,0)

if (!fadeout) a = max(a-0.005,0.25); else a = min(a+0.005,1);

l += 0.5;

print = string_copy(str,1,l);
if(l > string_length(str) +100) && (next < array_length_1d(strings)-1)

str = strings[next];